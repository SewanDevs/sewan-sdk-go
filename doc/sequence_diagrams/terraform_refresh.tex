\documentclass[landscape,a4paper,12pt]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{fullpage}
\usepackage{lscape}

\usepackage{tikz-uml}

\sloppy
\hyphenpenalty 10000000

\title{Sequence diagram : `terraform refresh` with sewan provider plugin and sdk}
\author{Sewan}

\begin{document}

\maketitle

\begin{center}
  \begin{tikzpicture}
    \begin{umlseqdiag}

    \umlactor[class=SysAdmin, x=-5]{admin}
    \umlobject[class=Tfstate, x=-1]{localstate}
    \umlobject[class=Terraform\&Plugin, x=5]{terraform}
    \umlobject[class=SewanSDK, x=10]{sdk}
    \umlobject[class=AirdrumAPI, x=14]{api}
    \umlobject[class=ResourceSchema,x=18]{schema}
    
    \begin{umlcall}[op=1.\ terraform\ refresh(), type=synchron, return=refresh\ status]{admin}{terraform}

      \begin{umlcall}[op=2.\ getLocalResourcesList(), type=synchron, return=resources\_list]{terraform}{localstate}
      \end{umlcall}

      \begin{umlfragment}[type=loop, label=resource\_in\_resources\_list, name=loop, inner xsep=10,fill=blue!15]
	\begin{umlcall}[op=4.\ create\_schema(local\_resource\_state), type=asynchron]{terraform}{schema}
	  \begin{umlcall}[op=3.\ resource\_read(), type=synchron, return=distant\_resource\_state]{terraform}{sdk}
	    \begin{umlcall}[op=4.\ HTTP\_GET(), type=synchron, return=HTTP.resp]{sdk}{api}
	    \end{umlcall}
	  \end{umlcall}
	  \begin{umlcall}[op=4.\ update\_schema(distant\_resource\_state), type=synchron, return=HTTP.resp]{terraform}{schema}
	  \end{umlcall}
	  \begin{umlcall}[op=5.\ update\_resource(schema), type=synchron, return=OK]{terraform}{localstate}
	  \end{umlcall}
	\end{umlcall}
      \end{umlfragment}

    \end{umlcall}

    \end{umlseqdiag}
  \end{tikzpicture}
\end{center}

\end{document}