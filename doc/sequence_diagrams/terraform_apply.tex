\documentclass[landscape,a4paper,12pt]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{fullpage}
\usepackage{lscape}

\usepackage{tikz-uml}

\sloppy
\hyphenpenalty 10000000

\title{Sequence diagram : `terraform apply` with sewan provider plugin and sdk}
\author{Sewan}

\begin{document}

\maketitle

\begin{center}

  \section{Infrastructure plan application}
  \begin{tikzpicture}
    \begin{umlseqdiag}

    \umlactor[class=SysAdmin, x=-3]{admin}
    \umlobject[class=Tfstate, x=0.5]{localstate}
    \umlobject[class=Terraform\&Plugin, x=6]{terraform}
    \umlobject[class=SewanSDK, x=11]{sdk}
    \umlobject[class=AirdrumAPI, x=15]{api}

    \begin{umlcall}[op=1.\ terraform\ refresh(), type=synchron, return=resources\_diff\_list]{admin}{terraform}
    
      \begin{umlcall}[op=2.\ plan(), type=asynchron]{terraform}{terraform}
      \end{umlcall}
    
      \begin{umlfragment}[type=loop, label=resource\_in\_plan, name=loop, inner xsep=15,fill=blue!15]

	\begin{umlfragment}[type=alt, label=resource\_exists, name=if, inner xsep=18,fill=white]
	  \begin{umlcall}[op=3.\ resource\_update(), type=synchron, return=resource\_state]{terraform}{sdk}
	    \begin{umlcall}[op=4.\ HTTP\_PUT(), type=synchron, return=HTTP.resp]{sdk}{api}
	    \end{umlcall}
	  \end{umlcall}

  	\umlfpart[!\ resource\_exists]

  	\begin{umlcall}[op=3.\ resource\_create(), type=synchron, return=resource\_state]{terraform}{sdk}
	  \begin{umlcall}[op=4.\ HTTP\_POST(), type=synchron, return=HTTP.resp]{sdk}{api}
	  \end{umlcall}
	\end{umlcall}

	\end{umlfragment}

      \end{umlfragment}[name=loop]
      
      \begin{umlcall}[op=5.\ update(), type=synchron, return=OK]{terraform}{localstate}
      \end{umlcall}

    \end{umlcall}

  \end{umlseqdiag}
  \end{tikzpicture}


  \section{VM resource creation from template}
  \begin{tikzpicture}
    \begin{umlseqdiag}
    \umlactor[class=SysAdmin, x=-3]{admin}
    \umlobject[class=Terraform\&Plugin, x=2]{terraform}
    \umlobject[class=SewanSDK, x=8]{sdk}
    \umlobject[class=AirdrumAPI, x=13]{api}

    \begin{umlcall}[op=1.\ resource\_create\_from\_template(template), type=synchron, return=resource\_state]{terraform}{sdk}
      \begin{umlcall}[op=2.\ HTTP\_GET\_AVAILABLE\_TEMPLATES\_LIST(), type=synchron, return=HTTP.resp]{sdk}{api}
      \end{umlcall}

      \begin{umlcall}[op=3.\ is\_template\_in\_list(), type=asynchron, return=HTTP.resp]{sdk}{sdk}
      \end{umlcall}

      \begin{umlfragment}[type=alt, label=template\_in\_list, name=if, inner xsep=18,fill=blue!15]
	\begin{umlcall}[op=4.\ update\_plan\_from\_template(), type=asynchron, return=HTTP.resp]{sdk}{sdk}
	\end{umlcall}
	\begin{umlcall}[op=4.\ HTTP\_PUT(), type=synchron, return=HTTP.resp]{sdk}{api}
	\end{umlcall}
      \umlfpart[!\ template\_in\_list]
	\begin{umlcall}[op=4.\ nullify\_plan()\ +\ resource\_state:error, type=asynchron, return=HTTP.resp]{sdk}{sdk}
	\end{umlcall}
      \end{umlfragment}[name=template]

    \end{umlcall}
    
    \end{umlseqdiag}
  \end{tikzpicture}

  
  \section{VM resource update  from template}
  \begin{tikzpicture}
    \begin{umlseqdiag}
    \umlactor[class=SysAdmin, x=-3]{admin}
    \umlobject[class=Terraform\&Plugin, x=2]{terraform}
    \umlobject[class=SewanSDK, x=8]{sdk}
    \umlobject[class=AirdrumAPI, x=13]{api}

    \begin{umlcall}[op=1.\ resource\_update\_from\_creation\_template\_data(), type=synchron, return=resource\_state]{terraform}{sdk}

	\begin{umlcall}[op=2.\ update\_plan\_from\_creation\_template\_data(), type=asynchron, return=HTTP.resp]{sdk}{sdk}
	\end{umlcall}
	\begin{umlcall}[op=3.\ HTTP\_POST(), type=synchron, return=HTTP.resp]{sdk}{api}
	\end{umlcall}

    \end{umlcall}
    
    \end{umlseqdiag}
  \end{tikzpicture}
  
\end{center}
\end{document}